

function  Tours (props)
{

    return (
    <> 
  <div>{props.name}</div>
        <img src={props.image}  />  
        </>
    
    )
}

export default Tours ;

{/* <div><img  src={props.img} alt={props.alt} />

    <p> {props.name}</p>
    
    </div>  */}